-- create the volume (once only)
mkfs.xtreemfs metadata.heavywater.ca/volume_name

-- create mount point (per client)
mkdir /mnt/xtreemfs

-- mount volume
sudo mount.xtreemfs -o=allow_other metadata.heavywater.ca/volume_name xtreemfs

-- unmount volume
fusermount -u /mnt/xtreemfs
